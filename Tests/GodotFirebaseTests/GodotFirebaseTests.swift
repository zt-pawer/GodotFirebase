import XCTest

final class GodotFirebaseTests: XCTestCase {
    func testPlaceholder() throws {
        // Firebase plugin tests require a running Firebase app.
        // Integration tests live in the companion Godot test project.
        XCTAssertTrue(true)
    }
}
